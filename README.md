# Installation

Stow all packages:

```bash
stow */ -t $HOME
```

Stow one package:

```bash
stow zsh -t $HOME
```

Or select the ones you want editing the `install` file and executing it:

```
./install
```
